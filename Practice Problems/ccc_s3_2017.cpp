#include <bits/stdc++.h>
using namespace std;

vector<int> lengths = {20, 30, 40, 30, 20, 35, 15, 10};
vector<vector<int>> boards;
double n = 8;

void find_seq(int l)
{
    if (end statement)
    {
        return;
    }
    else
    {
        for (int i = 0; i < n; i++)
        {
                }
    }
}

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    // cin >> n;

    // int temp;

    // for (int i = 0; i < n; i++)
    // {
    //     cin >> temp;
    //     lengths.push_back(temp);
    // }

    find_seq(floor(n / 2));

    return 0;
}