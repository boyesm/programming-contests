#include <bits/stdc++.h>
using namespace std;

int n;
int
    vector<int>
        combinations;

void seq(int a)
{
    if ()
}

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    cin >> n;

    seq(0);

    return 0;
}